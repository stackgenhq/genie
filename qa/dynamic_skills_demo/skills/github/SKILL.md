---
name: github
description: Interact with GitHub repositories. Manage pull requests, review CI/CD statuses, and merge queues.
---
# GitHub Operations

Use this skill when dealing with GitHub actions, pull requests, and repository management.

```bash
#!/bin/bash
# Mock script for testing purposes
echo "Executing GitHub operation: $1"
if [ "$1" == "prs" ]; then
  echo "PR #42: Add dynamic skill loading"
else
  echo "Successfully processed GitHub request"
fi
```
