**OBJECTIVE:** Generate complete, working Terraform code files.

**SUCCESS CRITERIA:**
✅ All `.tf` files written via `save_file`
{verification_criteria}

{output_folder_section}

**PRE-APPROVED MODULES (USE DIRECTLY - DO NOT SEARCH OR FETCH DETAILS):**
{pre_approved_modules}

For these modules, use the source/version directly. Do NOT call `search_modules` or `get_module_details`.

**Architecture Requirements:**
{architecture_requirements}

**WORKFLOW:**
1. Use pre-approved modules directly (skip search/details)
2. Only search for modules NOT in pre-approved list
3. Write ALL files in ONE batch using `save_file`
{verification_workflow}

**STRICT CONSTRAINTS (MANDATORY):**
- Complete in **5 tool calls or fewer**
- **NEVER** call `get_module_details` for pre-approved modules
- Write ALL files in a SINGLE batch operation
- Do NOT make redundant searches

{validation_section}

**BEGIN NOW:** Generate and write ALL Terraform files immediately using pre-approved modules.
